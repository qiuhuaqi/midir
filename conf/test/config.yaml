# @package _global_
defaults:
  - data: cardiac_ukbb
  - model: dl

metric_groups: ["disp_metrics", "image_metrics", "seg_metrics"]
evaluate: True
gpu:

base_dir:
model_base_dir:
model_dir: ${version_resolver:${base_dir}/${model_base_dir}}

hydra:
  run:
    dir: ${model_dir}/test