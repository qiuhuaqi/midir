# @package _global_
defaults:
  - data: cardiac_ukbb
  - model: dl

metric_groups: ["disp_metrics", "image_metrics", "seg_metrics"]
run_inference: True
run_evaluate: True
gpu:

model_base_dir:
model_dir: ${version_resolver:${model_base_dir}}
# resolver handles version selection under model dir

hydra:
  run:
    dir: ${model_dir}/test